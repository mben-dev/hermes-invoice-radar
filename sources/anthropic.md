# Anthropic

Method: email

- Each billing email carries 2 PDFs: the Invoice and the Receipt.
- Import only the Invoice; the Receipt is a duplicate for accounting purposes.
- Gmail query: `from:anthropic (invoice OR receipt)`, with a subject and date
  window (see emails/senders.md for the general rules).
