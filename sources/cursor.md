# Cursor

Method: user-provided PDFs

- Invoices are provided by the user (downloadable from the Cursor billing
  page, but the agent does not have access).
- The user drops the PDFs into the inbox folder, the adapter picks them up.
- Result: 2/2 matched.
