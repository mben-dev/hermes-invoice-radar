# Railway

Method: API (GraphQL)

- `POST https://backboard.railway.app/graphql/v2`
- Query type `CustomerInvoice` with a `pdfURL` field.
- Argument: `workspaceId` (workspace id anonymized).
- The PDF URL is directly downloadable.
- Result: 2/2 invoices matched.
